# Activated with Spring profile "e2e" (never use in production).
# Captures outbound HTML emails in memory for automated full-stack tests.
e2e.mail.capture=true
e2e.mail.hook-secret=${E2E_MAIL_HOOK_SECRET:}
